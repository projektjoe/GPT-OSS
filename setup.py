"""
Setup script for GPT-OSS educational implementation.

This is a minimal setup.py for compatibility.
All configuration is in pyproject.toml.
scikit-build-core handles the CMake build automatically.
"""

from setuptools import setup

# Configuration is in pyproject.toml
# scikit-build-core handles all CMake building
setup()
